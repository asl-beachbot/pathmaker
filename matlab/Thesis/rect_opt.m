% Optimize Rectangle fill of any shape

t = 0.2 % error threshhold


% idea: define atomic actions ... until threshold is reached
% 1. Split rectangle into two
% 2. Move rectangle
% 3. Vary size
% 4. Rotate rectangle
%    ...?
error = 10000;

while error > t
    method = rand
    % method 1: add random brush strokes
    if method > 0 && method < 1
        
    else
        
    end
end